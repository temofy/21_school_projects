#include "push_swap.h"

void	quick_sort(t_stack *a, t_stack *b)
{
	int 	pivot;
	int 	pivot_ind;
	int 	len;

	pivot_ind = a->size / 2;
	pivot = 0;

	if (pivot_ind > 1)
	{
		pivot = a->data[pivot_ind];
		int i = -1;
		while (++i < len)
		{
			if (i != pivot_ind)
			{
			}
		}
	}
}